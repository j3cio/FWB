interface MobileFilterButtonProps {
  icon?: boolean
  text: string
}

const MobileFilterButton = ({ icon, text }: MobileFilterButtonProps) => {
  return <div>MobileFilterButton</div>
}

export default MobileFilterButton
